// Archivo: Saludo.jsx

// 1. creamos la funcion (nuestro bloque de lego)

function saludo (){
  // 2. devolvemos lo que queremos que se vea en la pantalla
  return (
    <div>
      <h1>Hola, Universo!</h1>
      <p>Este es mi primer bloque de lego en react</p>
    </div>
  );
}

// 3. lo exportamos para usarlo en nuestra anve espacial (App Principal)
export default saludo;